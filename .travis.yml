language: python

python:
 - pypy
 - pypy3
 - 2.7
 - 3.4
 - 3.5
 - 3.6

sudo: false

install:
 - pip install pycodestyle pyflakes

script:
 # Pretend tests
 - pyflakes .
 - pycodestyle --statistics --count .

matrix:
  fast_finish: true
